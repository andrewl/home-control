services:
  control-panel:
    image: ghcr.io/andrewl/home-control:latest
    ports:
      - "8080:8080"
    volumes:
      - ./config.json:/app/config.json:ro
      - ./templates:/app/templates:ro
      - ./static:/app/static:ro
    restart: unless-stopped

